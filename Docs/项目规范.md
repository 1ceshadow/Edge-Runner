
## 🏷️命名
- **文件和文件夹**:不要在文件夹名中使用空格或特殊字符，使用大驼峰 PascalCase，脚本名应反映功能，如 `EnemyAI` 而非 `Script1`。
- **变量和方法**：变量用 camelCase（小驼峰，如 `playerHealth`），常量用 UPPER_SNAKE_CASE（如 `MAX_HEALTH`）。方法用动词开头，如 `InitializePlayer()`。

## 游戏参数

游戏内参数统一在一个脚本中设置，然后分发给各脚本
**调试参数**：
可以在unity编辑器中，运行并调试参数，但参数最后一定要修改到代码内。

## 🌴项目结构

结构不是固定不变的，怎么方便怎么来，可以随着项目扩展，重新组织。  
一般有两种结构，按相关性分类 和 按功能性分类

我们项目人少，然后有很多重复利用，所以就整体按照功能性(模块化)分类，细节处按照相关性分类    

下面是一个试用结构：
```bash
ProjectRoot/                        (项目根目录)
├── Assets/                         (核心资产文件夹，所有 Unity 导入的资源在此)
├── Docs/                           (项目文档)
│   ├── StyleGuide.md               (代码和命名规范)
│   └── Architecture.md             (架构图和设计文档)
├── .gitignore                      (版本控制忽略文件)
|   // 下面通常是自动生成的
├── Packages/                       (Unity Package Manager 管理的包，通常自动生成)
├── ProjectSettings/                (项目设置文件，如输入、标签等)
├── UserSettings/
├── Library/                        (Unity 缓存文件夹，忽略版本控制)
├── Temp/                           (临时文件，忽略版本控制)
├── Logs/                           (日志文件，忽略版本控制)
└── ProjectName.slnx
```

Assets 结构示例：
```bash
Assets/
├── Core/                                    ← 核心系统（纯代码+配置）
│   ├── Scripts/
│   │   ├── Manager/                         ← 全局管理器
│   │   │   ├── GameStateManager.cs
│   │   │   ├── AudioManager.cs
│   │   │   └── CameraController.cs
│   │   └── Data/                            ← 未来的 ScriptableObject 配置 (游戏的设置)
│   └── Settings/                            ← URP 和渲染设置 (项目的设置)
│       ├── UniversalRP.asset
│       └── Lit2DSceneTemplate.scenetemplate
|
├── Modules/
│   ├── Player/
│   │   ├── Scripts/
│   │   ├── Prefabs/
│   │   ├── Animations/      ← 只放 Player 专属动画
│   │   ├── Audio/           ← Player 脚步声、受伤音
│   │   └── Effects/
│   ├── Enemies/
│   │   ├── Animations/      ← 敌人专属
│   │   └── Audio/           ← 敌人音效
│   ├── Bullet/
│   │   └── Audio/           ← 射击音
│   ├── Audio/               ← 全局 Audio 模块
│   │   ├── Scripts/         ← AudioManager、MusicPlayer
│   │   ├── Music/           ← 所有 BGM
│   │   ├── SFX/             ← 共享音效（如按钮点击）
│   │   └── Prefabs/         ← AudioSource 预制体
│   ├── UI/
│   │   └── Animations/      ← UI 动画
│   └── Gameplay/
│       └── Effects/         ← 触发器特效
|
├── Art/                     ← 纯原始素材，无子 Animations/Audio
│   ├── Textures/            ←  Photos/Picture/Sprites 全扔这
│   ├── Models/
│   ├── Sprites/
│   │   └── Characters/
│   └── Animations/          ← 仅全局共享动画（极少用）
|
├── Scenes/
│       └── Levels/          如果关卡多
├── Prefabs/
└── 第三方资产包，如从 Asset Store 导入的，他们默认导入到某个位置，就不要再移动了 
```


### 相关链接
https://unity.com/how-to/organizing-your-project  

https://www.anchorpoint.app/blog/unity-folder-structure  

https://discussions.unity.com/t/best-practices-folder-structure/426959/8  


---

## 下面一般是编辑器默认的规范


## 📑代码格式化
- 使用空格，不用制表符
- 缩进级别为 4 个空格宽

- 括号风格：单独一行
```C#
// Avoid
if(playerWasHit) {
    PlaySound(playerHitSound);
    Damage(player, damageAmount);
}

// Prefer
if(playerWasHit)
{
    PlaySound(playerHitSound);
    Damage(player, damageAmount);
}
```
`public float Health { get { return health; } }`这种除外

- 条件语句后的每个语句都应位于括号内，即使它是单行语句；
```C#
// Bad
if(enemyInRange)
    Explode();

// Good
if(enemyInRange)
{
    Explode();
}
```

## 代码布局
- 库 using 指令应为文件的第一行，随后是类似 typedef 的 using 指令；
- 每个文件只定义一个类；文件名应与类名相同。

按照以下顺序定义一个类：
1. Nested classes  嵌套类
2. Constants  常量
3. Enums  枚举
4. Properties  属性
5. Fields  字段
6. Constructors 构造函数（如果适用）
7. Unity Messages  Unity 消息
8. Public methods  公共方法
9. Private methods  私有方法

推荐按以下顺序定义方法：
1. 初始化方法
2. 核心功能方法
3. 辅助或说明方法


---

## 项目结构的补充


```bash
Assets/
├── Core/                   (核心系统和框架，包含共享脚本和配置)
│   ├── Scripts/            (核心脚本，如游戏管理器、事件系统)
│   ├── Config/             (ScriptableObjects 或 JSON 配置文件)
│   └── Resources/          (动态加载的共享资源，如全局数据)
├── Modules/                (功能模块，按游戏系统分组，便于大型项目模块化)
│   ├── Gameplay/           (游戏核心逻辑)
│   │   ├── Scripts/        (如 PlayerController.cs)
│   │   ├── Prefabs/        (预制体，如 PlayerPrefab)
│   │   ├── Scenes/         (相关场景，如 Level1.unity)
│   │   └── Assets/         (模块特定资源，如模型、纹理)
│   ├── UI/                 (用户界面模块)
│   │   ├── Scripts/        (UI 脚本，如 MenuManager.cs)
│   │   ├── Prefabs/        (UI 预制体)
│   │   └── Assets/         (UI  sprites 和字体)
│   ├── Audio/              (音频模块)
│   │   ├── Scripts/        (音频管理脚本)
│   │   └── Sounds/         (音频文件，如 BGM、SFX)
│   └── Networking/         (如果有多人功能)
│       ├── Scripts/        (网络脚本)
│       └── Prefabs/        (网络相关预制体)
├── Art/                    (美术资源，按类型细分)
│   ├── Models/             (3D 模型)
│   ├── Textures/           (纹理和材质)
│   ├── Sprites/            (2D 精灵)
│   └── Animations/         (动画剪辑)
├── Scenes/                 (所有场景文件，按级别或类型分组)
│   ├── Main/               (主场景，如 Menu.unity)
│   └── Levels/             (关卡场景，如 Level01.unity)
├── Prefabs/                (共享预制体，不属于特定模块的)
|
|
├── Plugins/  (Unity 插件和扩展)
└── 第三方资产包，如从 Asset Store 导入的，他们默认导入到某个位置，就不要再移动了 

```

```bash
Assets/
├─ Core/                  ← 全局共享、基础系统 纯代码 + 配置
│   ├─ Scripts/
│   │   ├─ Manager/       ← GameManager, SaveSystem, EventSystem 等
│   │   ├─ Utilities/     ← 扩展方法、工具类
│   │   └─ Data/          ← ScriptableObject 全局配置
│   └─ Settings/          ← InputSystem 设置、Project Settings 资产等
│
├─ Modules/               ← 按功能模块划分
│   ├─ Player/
│   │   ├─ Scripts/      ← PlayerController, PlayerHealth 等
│   │   ├─ Prefabs/       ← Player 主预制体及变体
│   │   ├─ Animations/    
│   │   ├─ Audio/         
│   │   └─ Effects/       ← 粒子、特效
│   │
│   ├─ Enemies/           ← 所有敌人共用一个模块
│   │   ├─ Scripts/
│   │   ├─ Prefabs/       ← 各种敌人预制体
│   │   ├─ Animations/
│   │   └─ Data/          ← 敌人配置表（ScriptableObject）
│   │
│   ├─ Bullet/            
│   ├─ Input/             ← 输入系统
│   ├─ UI/                
│   │   ├─ Scripts/
│   │   ├─ Prefabs/       ← 所有 UI 页面、弹窗
│   │   ├─ Fonts/
│   │   └─ Atlases/       ← UI 图集
│   ├─ Audio/             ← 全局音效管理 + 所有音效文件
│   ├─ Gameplay/          ← 关卡逻辑、触发器、互动物体（原来的 Triggers 可以放这）
│   └─ Rendering/         ← 后处理、自定义 Shader、Lighting 设置
│
├─ Art/                   ← 纯美术资源
│   ├─ Materials/         ← 所有材质（可以再细分 PBR/URP/UI 等）
│   ├─ Textures/          ← 原始贴图（原来的 Photos/Picture 全部扔进来）
│   ├─ Sprites/
│   │   ├─ Characters/
│   │   ├─ Environment/
│   │   │   ├─ Ground/
│   │   │   ├─ Wall/
│   │   │   └─ TileSets/
│   │   └─ UI/
│   ├─ Models/            ← FBX、OBJ
│   └─ Animations/        ← 动画控制器、Animator Override（不属于特定模块的）
│
├─ Scenes/
│   ├─ Bootstrap.unity   ← 启动场景（只加载各种 Manager）
│   ├─ MainMenu.unity
│   └─ Levels/
│       ├─ Level_01.unity
│       └─ Level_02.unity
│
├─ Prefabs/               ← 全局共享预制体（不属于任何模块的）
└─ Plugins/               
├─ Packages/                  ← UPM 包
├─ TextMesh Pro/ 
│   ├─ Examples & Extras/
│   ├─ Fonts/
│   ├─ Shaders/
│   └─ ...
│
└─ External/                  
```